@using MudBlazor
@using CastlePlus2.Contracts.DTOs.Finanse

<MudDialog>
    <DialogContent>
        <MudForm @ref="_form">
            <MudText Typo="Typo.h6" Class="mb-3">
                @(_isReadOnly ? "Podgląd rozliczenia" : _isEdit ? "Edycja rozliczenia" : "Nowe rozliczenie")
            </MudText>

            <MudGrid Spacing="2">
                <MudItem xs="12">
                    <MudNumericField T="long"
                                     Label="IdPłatności"
                                     Dense="true"
                                     Required="true"
                                     Disabled="IsReadOnly"
                                     @bind-Value="Model.IdPlatnosci" />
                </MudItem>

                <MudItem xs="12">
                    <MudNumericField T="long"
                                     Label="IdFaktury"
                                     Dense="true"
                                     Required="true"
                                     Disabled="IsReadOnly"
                                     @bind-Value="Model.IdFaktury" />
                </MudItem>

                <MudItem xs="12">
                    <MudNumericField T="decimal"
                                     Label="Kwota"
                                     Dense="true"
                                     Required="true"
                                     Disabled="IsReadOnly"
                                     @bind-Value="Model.Kwota" />
                </MudItem>

                @if (_isEdit)
                {
                    <MudItem xs="12">
                        <MudText Typo="Typo.caption" Color="Color.Secondary">
                            IdRozliczenia: @Model.IdRozliczenia
                        </MudText>
                    </MudItem>
                }
            </MudGrid>
        </MudForm>
    </DialogContent>

    <DialogActions>
        <MudButton OnClick="Cancel">@(_isReadOnly ? "Zamknij" : "Anuluj")</MudButton>
        @if (!_isReadOnly)
        {
            <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="SaveAsync">Zapisz</MudButton>
        }
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] private IMudDialogInstance MudDialog { get; set; } = default!;

    [Parameter] public RozliczeniePlatnosciDto Model { get; set; } = new();
    [Parameter] public bool IsEdit { get; set; }
    [Parameter] public bool IsReadOnly { get; set; } = false;

    private MudForm? _form;
    private bool _isEdit => IsEdit;
    private bool _isReadOnly => IsReadOnly;

    private void Cancel() => MudDialog.Cancel();

    private async Task SaveAsync()
    {
        if (_isReadOnly)
            return;

        if (_form is null)
            return;

        await _form.Validate();
        if (!_form.IsValid)
            return;

        MudDialog.Close(DialogResult.Ok(Model));
    }
}
